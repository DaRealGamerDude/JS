"use strict"; // treat all JS code as newer version

// alert(3 + 3)
// We are using NodeJS, not browser

console.log(3+3);           //code readability should be high
console.log("Bhavit"); 

let name = "Bhavit"   //string 
let age = "18"    //integer
let isLoggedIn = false //or true- boolean datatype
let state;

// number => 2 to power 53 (IEEE 754 double-precision floating point numbers)
// any number greater than that, we can use bigint
// string should be used with " " although '' can also be used
// boolean => true/false
// null => standalone value 
// undefined
// symbol => uniqueness (used mainly in react)
// IMP- object

console.log(typeof null); // type is object
console.log(typeof undefined); // undefined

